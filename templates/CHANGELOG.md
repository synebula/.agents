# CHANGELOG.md 模板

```markdown
# 项目变更历史

> 记录架构调整、重要功能上线、技术选型变更，按时间倒序（最新在前），按需查阅

---

## 2025-11-26 14:30 - 认证模块JWT迁移

**类型**：架构重构 | **范围**：`backend/auth/`  
**原因**：Session 内存存储导致多实例会话不共享

**改动**：

- 登录接口返回 JWT token（有效期7天）
- 新增 `AuthMiddleware` 验证 + `refresh_token` 表
- 兼容模式：1周过渡期同时支持 session/JWT

**文档**：`.agents/TASK-JWT迁移.md`

---

## 记录规范

**必选字段**：时间、标题、类型、范围、原因、改动、文档链接  
**可选字段**：技术细节、决策理由、性能数据

**更新原则**：

- ✅ 架构级变更、重要功能
- ❌ 日常 bugfix、实验性功能（稳定后再加）
```
